# DayOne to Obsidian Converter - Configuration
# Copy this file to .env and customize the values for your setup

# ===== File Paths =====
# Default input JSON file from DayOne export
INPUT_FILE=DayOne Journal.json

# Output directory for converted files
OUTPUT_DIR=./output

# Subdirectory name for journal entries (within output directory)
JOURNAL_ENTRIES_SUBDIR=journal-entries

# Subdirectory name for photos (within journal entries directory)
PHOTOS_SUBDIR=photos

# Log file name (created in output directory)
LOG_FILENAME=conversion_log.txt

# ===== Timezone Settings =====
# Your local timezone (must be one of: America/Chicago, America/New_York, 
# America/Denver, America/Los_Angeles, America/Phoenix, UTC)
# Default: America/Chicago
DEFAULT_TIMEZONE=America/Chicago

# ===== Filename Settings =====
# Maximum length for generated filenames (characters)
MAX_FILENAME_LENGTH=50

# UUID prefix length in filenames (characters from UUID)
UUID_PREFIX_LENGTH=8

# ===== Image Settings =====
# Default image file extension
DEFAULT_IMAGE_EXTENSION=jpeg

# ===== Date/Time Formats =====
# Format for dates in filenames (Python strftime format)
DATE_FORMAT_FILENAME=%Y-%m-%d

# Format for datetime in frontmatter (Python strftime format)
DATETIME_FORMAT_FRONTMATTER=%Y-%m-%dT%H:%M:%S

# Format for log timestamps (Python strftime format)
LOG_TIMESTAMP_FORMAT=%Y-%m-%d %H:%M:%S

# ===== Content Settings =====
# Fallback title for entries without a title
FALLBACK_TITLE=Untitled
